const fs = require('fs')

solve = d=>d.split('\n').map(r=>r.split(' ').map(i=>[...i].sort().join())).filter(b=>(new Set(b).size==b.length)).length

fs.readFile('i', 'utf-8', (e,d) => console.log(solve(d)))