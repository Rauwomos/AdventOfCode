const fs = require('fs')

solve = d => {

    let inGarbage = false

    // Remove all garbage
    for (i=0; i<d.length; i++) {
        if(inGarbage) {

        } else {

        }
    }

    return max

}

fs.readFile('i', 'utf-8', (e,d) => console.log(solve(d)))